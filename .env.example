# Copy these names into GitHub Actions secrets (Repository → Settings → Secrets → Actions).
# Never commit real values.

# Default provider (vercel-ai-gateway)
AI_GATEWAY_API_KEY=

# typesafe-native provider
TYPESAFE_API_KEY=

# custom-compatible provider
JEV_CUSTOM_API_KEY=
